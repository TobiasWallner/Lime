#pragma once

#include <cinttypes>

class string_view{
private:
	char* _ptr;
	std::uint32_t _byte_size;
	std::uint32_t _size;
	
public
	
	string_view(char* ptr, std::uint32_t byte_size, std::uint32_t size) : 
		_ptr(ptr), _byte_size(byte_size, _size(size);
		
	

	/*TODO*/
}